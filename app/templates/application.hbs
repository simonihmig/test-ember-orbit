<button {{action "createPatient"}}>CREATE PATIENT</button>
{{#each patients as |record|}}
  {{log record.remoteId}}
<ul>
  <li>id: {{record.id}}</li>
  <li>name: {{record.name}}</li>
  <li>remoteId: {{record.remoteId}}</li>
</ul>
{{/each}}
